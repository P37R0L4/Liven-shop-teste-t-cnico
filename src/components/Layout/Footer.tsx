import { Center, Divider, HStack, VStack } from "@chakra-ui/react";

export default function Footer() {
  return (
    <Center w="full" h="20rem" bg="white" flex="1" shadow="base">
      <HStack w="40rem" mx={5} my={5}>
        <VStack>

        </VStack>

        <Divider orientation="vertical" />
        <VStack>

        </VStack>
      </HStack>
    </Center>
  )
}
